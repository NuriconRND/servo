# SDD Progress Ledger — DComp 스왑체인 콘텐츠 + 레이어 컬링

- Plan: docs/superpowers/plans/2026-07-14-dcomp-swapchain-content.md
- Spec: docs/superpowers/specs/2026-07-14-dcomp-swapchain-content-design.md
- Branch: multigpu-tiled-wall
- BASE (branch work start): 3b299f196
- 핵심 규칙: Present는 full coverage(타일 단위 valid 피복)에서만 — 잔상 불허·지연 허용. 알파 서피스 컬링 금지. =surface = 구 경로 바이트 동일.

## Tasks (스왑체인 콘텐츠)
(진행되는 대로 아래에 한 줄씩 추가)
- 최종 whole-branch 리뷰(fable): **With fixes → 픽스 완료(21f6dab41)**. Critical 0, Important 2건 처리: ①알파 컬링 재해석(주체=불투명 조건, 완전 피복 알파는 비가시라 제외 = 시각 등가 — 계획 문안과 일치, 스펙 문면만 이탈) → 스펙 §5.2/§5.3 정정+§10 공시 ②비조밀 타일 그리드 조밀성 가드(FLIP_DISCARD 미정의 픽셀 이론 경로 차단, 3줄). Minor: 헬퍼 무경고 서브패스 warn 추가, =surface "storage만 legacy" 문구 명시(런처 2종). 리뷰 하이라이트: displayed_anchor 3상태 완전성·자원 수지·WR 계약(이연 AddVisual vs 디버그 오버레이 호출 순서 renderer/mod.rs:1898→1913) 외부 코드 검증. 트리아지: (b)(g)(h) 이미 해소/무시, (c)(d) 후속 웨이브 묶음, (i)(j) 이월 유지. 패키지 재갱신(exe SHA 교체, 1.2GB). ★전 태스크+최종 리뷰 완료, HEAD 21f6dab41, 10커밋(03946de91..21f6dab41) 로컬 미푸시 — 사용자 결정 대기.★
- Task 6: complete (commit 763748af3 — dev 런처 -DCompSurface(hybrid/surface/off 3분기+마커) + 스펙 §10 구현결과/이탈). run_wall.ps1(리포 밖) 동일 갱신+AMD 3중 A/B 가이드. ServoWallPackage.zip 재생성(1146MB, exe e8b0561ce). 스모크 dev 2종+패키지 2종 전부 PASS(hybrid promote≥1 / surface promote=0). 남은 것: 최종 whole-branch 리뷰 → 사용자 푸시 게이트.
- Task 5 픽스 웨이브: complete (commit e8b0561ce, review clean 승인). Part A=is_opaque 승격 게이트(설계 §5.3 정합) — overlay 오승격/withhold 병리 완전 소멸, 불투명 경로 무회귀(45타일 promote=2 유지). Part B=비불투명 슬라이스 비디오 검정 근본원인 규명·이월: 가설 전부 실험 기각(config alpha=8 확인, FORCE_IGNORE 무효, ★비디오 없는 비불투명 솔리드는 정상 표시★) → 결함은 "비불투명 픽처캐시 슬라이스의 비디오(D3D11/YUV external) 합성 경로"(WR측, =surface 베이스 공유, 이번 회귀 아님) — 다음 단계 3종 보고서 명시. ★Ctrl+F12는 다른 계열로 판명(비불투명 솔리드가 표시되므로 알파 문제 아님 — 프로파일러의 메인 프레임버퍼 드로우가 present 스킵으로 미표시, egui 크롬 우회 계열).★ 잔여 withhold=305(45타일)는 30fps 콘텐츠의 부분 dirty 프레임 보류 = 정상 케이던스.
- Task 5: 검증 완료·코드변경 0 (report=task-5-report.md). 5-1·5-2·5-4·5-5·5-6 **PASS**: 4분면 승격+픽셀+리사이즈 regen, 45타일 lockstep±0·블랙0·5분 메모리 플랫(~4790MB)·perf avg60.97/drop0, 3모드 매트릭스(off/surface/on 전부 렌더·fps~61), PresentMon =1 1545 Present(Composed:Flip)↔=surface 0(대조 성립), 해체3+재기동 크래시0. **★5-3 FAIL(텍스트 오버레이 검정)★**: 근본원인=이 float 레이아웃에서 WR이 라이브 비디오를 **비불투명 슬라이스**에 배치(스펙 §5 line19 위반) → DComp가 비불투명 슬라이스 비디오 미표시. **=surface(구 머지된 베이스 경로)에서도 동일 재현 → 본 프로젝트 Task1-4 회귀 아님·승격 무관**, 프로덕션 flex-wrap 월은 비디오=불투명 슬라이스라 3모드 전부 정상. 부수발견=승격 판정식(dcomp_compositor.rs ~996) is_opaque 게이트 부재로 알파 슬라이스 오승격+withhold 병리(고쳐도 =surface 검정이라 증상 미해소). 브리프 "임의 우회 금지"대로 투기적 수정/커밋 없이 회부. 권고: ①(범위밖·근본) 비불투명 슬라이스 비디오 DComp 표시 ②(범위내·저위험) is_opaque 승격 게이트.
- Task 4: complete (commit 461ee73dc, review clean 1차 승인·픽스 0). AddVisual 이연+컬링: device 클립 입력 확인, Present는 컬링 무관 지속(트리 제외≠갱신 중단), 45타일에서 cull id=0 관측+픽셀 정상, NO_CULL 스위치 동작. Minor(이월): 동일 프레임 add_surface↔destroy 레이스 시 죽은 클립이 컬링 계산에 참여(WR 계약상 미발생, 이론), add_surface root 가드 제거(정당 확인됨, 브리프 외 소폭 변경).
- Task 3: complete (commits 83e7e5c61..f43e8ce24 = 본 1f1282dff + 픽스 f43e8ce24, review 재리뷰 Approved). 하이브리드 결선: 3연속 전면갱신 승격, full-coverage Present(타일 단위), 첫 완전 Present에서 SetContent+오프셋 원자 전환. ★브리프 외 근본수정 2건(리뷰 검증 통과): ①add_surface anchor — 리뷰 지적(승격~첫Present 창구간에서 fallback이 ~16384px 밖 소실)을 `displayed_anchor: Option<DeviceIntPoint>` 3상태(fallback/구스왑체인/현스왑체인)로 일반화 해결 — 렌더측 anchor와 표시측 anchor 분리, 리뷰어가 '제안보다 엄밀히 우수' 판정, 재현 페이지(quad_promote_partial.html?full=N)로 창구간 711프레임 유지+픽셀 정확 직접 검증 ②리사이즈 regen — ★WR은 창 리사이즈에서 서피스를 재생성하지 않음(실측)★ → extent 변화 감지 재생성(무글리치: 구 스왑체인이 DComp ref로 표시 유지). ★함정: 요소의 첫 transform 부여가 슬라이스 전면 무효화 1프레임 유발(재현 페이지 설계 시 발견).★ Minor(이월): 전역 실패 게이트(promote/regen)가 서피스 전체 동결, 동결 상태 반복 진단 부재, content-swap시 last_placement None 이론 경로.
- Task 2: complete (commit 39adfd002..83e7e5c61, review clean 1차 승인·픽스 0). 리팩터 등가성·COM 수명(어댑터 임시/팩토리 영구)·winapi 시그니처 전부 독립 검증. 3모드 스모크: =1/=surface 픽셀 바이트 동일, off 정상. Minor(이월): release_all/destroy_surface 주석 stale(virtual_surface→storage — Task 3에서 그 코드 만질 때 갱신), SurfaceEntry.frame_coverage는 Task 3 몫(Task 2 브리프에 없음이 정상).
- Task 1: complete (commit 3b299f196..39adfd002, review clean 1차 승인·픽스 0). 리뷰어가 euclid 0.22.14 소스로 contains_box 방향 검증 + cull 4케이스 수동 추적. surfman truthy 추가는 증명적으로 additive. 테스트: surfman present_path 4/4(GPU hang 우회=바이너리 직접 실행), paint는 선재 feature-unification(create_isolated_device — 리뷰어가 선재성 독립 확인). Minor(이월): 테스트 내 unwrap 1건(brief-mandated, CI 클리피 미도달 — 최종 리뷰 트리아지). ★환경 지식: surfman GPU 테스트는 이 샌드박스에서 상시 hang — 필터/직접실행으로 우회할 것.

---

# (완료) SDD Progress Ledger — WR Native Compositor (DComp)

- Plan: docs/superpowers/plans/2026-07-13-wr-native-compositor.md
- Spec: docs/superpowers/specs/2026-07-13-wr-native-compositor-design.md
- Branch: multigpu-tiled-wall
- BASE (branch work start): 011621190
- ★HALT 규칙: Task 3 PoC 게이트 4종(G1 DComp 디바이스/타깃, G2 BeginDraw 텍스처 pbuffer 래핑, G3 Commit 표시, G4 top-left 방향) 중 하나라도 FAIL이면 이후 태스크 중단 + 사용자 문의

## Tasks (WR Native Compositor)
(진행되는 대로 아래에 한 줄씩 추가)
- 후속(사용자 요청, 2026-07-13 밤): 하단 검정 밴드 제거 — 원인=egui **top 패널**(툴바+탭)이 웹뷰를 아래로 밀고 DComp는 창 원점부터 그려 하단이 미커버. 수정=게이트 요청(env) 시 egui 크롬 패널 미배치(웹뷰 풀윈도우; env 판정 사유=레이아웃은 시작부터 안정 필요+폴백에서도 안전). 커밋 ec399ddab(gui.rs+servoshell Cargo에 paint_api 의존)+56eeb1fd4(스펙 §12) → PUSHED(cc170b23e..56eeb1fd4). 45타일 실기 재확인: 하단 가장자리까지 영상 픽셀(밴드 0px). 패키지 재갱신(exe 21:16, zip 1.20GB). ★검증 함정: 1080p 모니터에서 타이틀바가 클라이언트를 화면 밖으로 밀면 CopyFromScreen이 가짜 검정 '밴드' 캡처 — 온스크린 배치 후 판정할 것.★
- ★전체 완료·PUSHED (2026-07-13): 11d2a274d..cc170b23e 17커밋 → nuriconrnd/multigpu-tiled-wall.★ 사용자 결정 3건 처리: ①푸시 ②egui 잔여 blit 지금 수정(servoshell 무변경 조항 사용자 승인 해제 — 35fa1f7df getter dcomp_native_active+gui.rs 가드, cc170b23e 스펙 갱신; 리뷰 승인 이슈 0; 부수효과: DComp 전체커버 contentTop=0으로 하단 흰 밴드도 소멸; 게이트 on 앱측 트래픽 = 콘텐츠 draw 1× probe 패리티 달성) ③Ctrl+F12 후속 이월. ServoWallPackage.zip 최종 재패키징(blit 스킵 exe 포함, 20:33, 1,198,917,737B) + 단독 스모크 PASS.
- 최종 whole-branch 리뷰(fable): **Ready to merge: YES** (Critical 0). Important 3건 처리: ①잔여 egui blit(gui.rs:622-636, 게이트 on에서도 콘텐츠 없는 오프스크린→백버퍼 blit ~1× 잔존 → AMD 합계 ~2×; servoshell 무변경 조항 충돌로 ★사용자 결정 대기★, §12 공시됨) ②스크린샷 사일런트 오답(§12 공시로 종결 — §3 비범위 정합; warn은 ScreenshotTaker private라 미구현, 사유 기록) ③디바이스 로스트 로그 폭주(후속 이월). Minor 트리아지 완료(대부분 이월/무시, set-once는 픽스로 해소). 픽스 커밋 4d3fc048a(+18: 폴백 시 set_dcomp_native_active(false) 명시 + §12 공시 2건), 빌드 Succeeded + ON/OFF 스모크 재확인. ★HEAD 4d3fc048a, 총 13커밋(011621190..4d3fc048a), 전부 로컬 미푸시 — 사용자 결정 대기.★
- Task 7: complete (commit 58e413142 — 스펙 §12 구현결과/이탈 + run_video_wall_d3d11.ps1 -DComp 스위치+마커 검증). ServoWallPackage 재패키징: exe(HEAD 2f0f449d4 빌드) 교체 + run_wall.ps1 -DComp + AMD 판독 가이드, zip 1143MB 재생성, 패키지 단독 스모크 PASS(-DComp 마커+비디오 픽셀). 스모크 3종 전부 PASS. 남은 것: 최종 whole-branch 리뷰(컨트롤러) → 사용자 푸시 게이트.
- Task 6: complete (코드 변경 0, 검증 전용). 7/8 PASS: 2×2 색/방향/lockstep±0, 45타일 45/45·lockstep±0~1·블랙0·5분 메모리 플랫, ②단 소멸 존재증명(전 타일 DComp add_surface+present 스킵; GPU%는 A5000 decode 지배로 무회귀만 확인 — 실이득은 AMD 몫), WebGPU 월 70-116fps 블랙0, 저더 fps avg 61.5/drop 0, off 회귀 동일, 리사이즈/이동/종료 무크래시. ★유일 FAIL(6-5): 게이트 on에서 Ctrl+F12 오버레이 미표시 — 근본원인 = 비불투명 DEBUG_OVERLAY 서피스 premultiplied 알파 합성(서피스 처리 자체는 정상, off에선 표시됨). 진단 전용이라 월 무영향 — 컨트롤러 판단: 콘텐츠 경로 회귀 리스크 > 진단 이득으로 후속 이월, 스펙 §10 이탈 명시 + 최종 사용자 게이트에서 결정. 증거 13파일 evidence/.
- Task 5: complete (commits 187c84541..2f0f449d4 = 6커밋: 결선 1f4fd0d2f + Offscreen hwnd 위임 1759ce068 + 진단 aaf43549e + ★ppf 게이트 d0486e4a3★ + present 스킵 e87765943 + 리뷰픽스 2f0f449d4, review 재리뷰 Approved). ★DComp 경로 servoshell 최초 실표시 + 수직 정렬 버그 근본수정: ANGLE ppf가 pbuffer(GL_FRAMEBUFFER_DEFAULT)에도 발동(UsePresentPathFast renderer11_utils.cpp:2677, viewScale+1+시저 y 반전 StateManager11.cpp:535/1416)해 WR NativeSurface top-left 규약(stock ANGLE 전제)을 깨뜨림 → 게이트 on이면 ppf 디스플레이 속성 제외 + 창 present 스킵(실발동 기준 Cell 플래그, env 아님 — 폴백 블랭크 방지). Gecko가 동작하는 이유=ppf 자체를 안 씀.★ 스모크: ON 4분면 픽셀 정확(1280x720+리사이즈 1600x920), OFF 무회귀, 마커 2종, 클린 종료. 함정: PoC는 clear+자체 시저 flip이라 ppf 방향 문제를 검증 못 했음(상쇄). Minor(이월): set-once 플래그 주석, no-wgl↔sm-angle cfg 커플링, 정적 페이지 미재합성(검증은 애니메이션/비디오 페이지로), 게이트 on 시 하단 툴바 높이 흰 밴드(스코프 허용), M1 단위테스트 선재 feature-unification.
- Task 4: complete (commit 187c84541, review clean 1차 승인·픽스 0). 리뷰어가 trait 시그니처를 composite.rs와, DComp 시퀀스를 PoC와 대조 확정; COM refcount 전 경로(4개 bind 실패 경로 포함) 균형 추적 검증; deinit/Drop 이중해제 안전(idempotent release_all). 이탈 3건 전부 정당 판정(BoundTile 확장=unbind 계약상 필수, winapi 44피처, servoshell check 게이트 대체). Minor(이월): M1 단위테스트 미실행(Task 5에서 실행할 것), M2 bind 재진입 warn 부재(WR 1:1 계약 의존), M3 지속 실패 시 로그 폭주 가능, M4 SetClip 좌표계는 Task 5 스모크가 실검증, M5 is_opaque 필드 dead code.
- Task 3: complete (commit cc407c7ea, review clean 1차 승인·픽스 0). ★PoC 게이트 4/4 PASS (RTX A5000 실기, 픽셀 오차 0) — HALT 불필요, G2(BeginDraw 텍스처 pbuffer 래핑=최대 리스크) 통과.★ ★결정적 발견: ANGLE 기본 창 서피스가 HWND DComp 타깃 선점(0x88980800 WINDOW_ALREADY_COMPOSED) → SERVO_COMPOSITOR_DCOMP=1 opt-out(Task 1)이 CreateTargetForHwnd 성립의 필수 전제. Servo 통합에선 env가 프로세스 시작 전 설정되므로 자연 충족.★ 리뷰어가 in-code env 설정을 '게이트 약화 아닌 정당한 전제조건'으로 판정(G3/G4는 실화면 GetPixel이라 스푸핑 불가). winapi 피처는 브리프 최소목록 컴파일 불가 → dev-deps에 전이폐포 44개(프로덕션 deps 무영향). Minor(이월): UnregisterClass 미호출, 조기 ? 경로 COM 누수(PoC 허용범위). Task 4 주의: paint 크레이트 프로덕션 winapi 피처는 별도 추가 필요.
- Task 2: complete (commits 27ac77631..a29f6cab6 = 290a4a060 구현 + a29f6cab6 import cfg 픽스, review 2차 승인). cargo check -p servo-paint-api 0에러. 브리프 이탈 2건 정당 확인(UntypedSize2D 파일 관례, EGLSurface 미노출→포인터캐스트 관용구). Minor(이월): set_window가 win32_hwnd 미갱신(월 시나리오 미발생, Task 4/5 인지사항), 비Windows는 구조적 검증만(cross-target 부재).
- Task 1: complete (commit 011621190..27ac77631, review clean 1차 승인·픽스 0). cargo check -p surfman 0에러. 게이트off 바이트동일을 리뷰어가 수동 trace로 독립 검증. Minor(이월): dcomp_native_compositor_requested() 2회 호출 스타일, destroy_render_pbuffer 실패 로그 부재(브리프 문자 그대로라 이탈 아님).

---

# (완료) SDD Progress Ledger — WR YUV 직접 샘플 (A-dyn)

- Plan: docs/superpowers/plans/2026-07-12-wr-yuv-direct-sample.md
- Spec: docs/superpowers/specs/2026-07-12-wr-yuv-direct-sample-design.md
- Branch: multigpu-tiled-wall
- BASE (branch work start): 1c3ba3845
- 상태: 전체 완료·PUSHED (10-bit 포함, HEAD 11d2a274d)

## 이전 계획 (완료·푸시됨)
- 2026-07-11-angle-present-path-fast 및 그 이전: 전체 완료·PUSHED (상세는 git log 및 메모리 §3-p)

## 환경 함정 (전 태스크 적용)
- ★`cargo check --workspace` 및 script 계열 크레이트의 맨 cargo check는 이 머신에서 **불가** — debug 프로파일이 mozjs_sys(SpiderMonkey) 빌드 스크립트를 처음 빌드하려 하고, mach의 mozbuild 환경 없이는 에러 대신 **행**(CPU ~0, rustc 자식 0). 워크스페이스 게이트는 `.\mach build --release`(servo_env 소싱)로 대체. 계획 Task 5·6의 'cargo check --workspace' 스텝은 이걸로 치환. leaf 크레이트(-p surfman/servo-paint-api/servo-media-player 등 mozjs 미의존)는 cargo check/test 가능.
- 동일 target 디렉터리에 cargo 2개 동시 실행 금지(락 경합 정지). 장기 실행은 출력을 파일로 리다이렉트(파이프 배압 방지).

## Tasks
(진행되는 대로 아래에 한 줄씩 추가)
- Task 1: complete (commits 1c3ba3845..00fbe0790 = f42e8c470 구현 + 00fbe0790 EGLImage 누수 픽스, review clean). cargo check -p surfman 0 에러. Minor(최종 리뷰로 이월): destroy_gl_texture_and_egl_image가 make-current 실패 시 무음 no-op(브리프 시그니처 그대로); eglext 문서 인용 리포 내 미검증.
- Task 2: complete (commit 80fa561e4, review clean, 픽스 0). 7메서드×5지점(트레이트+Surfman실구현+위임3) 리뷰어 grep 독립 검증. AddRef 계약 분리(surfman은 안 함/paint는 함) 확인. 패키지명=servo-paint-api, winapi unknwnbase feature 추가. Minor(이월): 짝 메서드(unmap/destroy) 트레이트 doc에 스레드 계약 미전파.
- [후속] 10-bit 지원: complete (commits 1c00a6dd2+5ab120299+11d2a274d, review clean 1차 승인). I420_10LE(R16×3, Color10)+P010_10LE(R16+RG16, Color16, WR YuvData::P010) — ColorDepth 페어링을 리뷰어가 yuv.glsl 수식 레벨로 독립 재검증(endpoint 정확 일치, Gecko 관례 동일). jellyfish HEVC 10bit 실기 검증(negotiated I42010le, 색 정상). P010은 라이브 미발화(이 장비 SW 디코더가 I420_10LE 출력 — 대칭성+소스도출로 수용, 문서화됨). 테스트 페이지 ?src= + 런처 -Src 추가. Minor(이월): 동일 해상도 8↔10bit 전환이 같은 compat family로 키 재사용(UpdateImage 디스크립터 교체에 의존 — 희귀 시나리오), 리포트의 rescaling_factor 인용은 corroborative(operative는 bit_depth/channel_max). ★미푸시★
- Task 9: complete — ★전체 완료, 최종 리뷰(fable) Ready to merge: YES★. 문서 커밋 5b91df1ed(스펙 §10 구현결과/이탈 + 패키징 노트). 최종 whole-branch 리뷰(1c3ba3845..5b91df1ed)가 통합 이음새 결함 3건 발견 → 픽스 웨이브 3커밋(371a0a564 Drop 링 누수 / 09a89a428 스테일 포인터 무효화+슬롯0 unmap[스펙 §4.3 자체 결함 정정] / 2ae8a50f6 주석·스펙 정합) → 재리뷰 전 항목 FIXED 확인, 조합 정합성 검증(2단계 Advance 회귀 테스트 red-check 포함). 게이트: 레지스트리 11/11, 프로듀서 3/3, mach EXIT 0, 실기 2×2+45타일 재검증 클린. 잔여(전부 triage됨, 비차단): 비PROF 배압 warn 부재(§10.4 문구 미세 과장), abandon_slot dead code, 바인딩 vestigial 필드, caps변경 purge 레이스, vendored surfman 이중 eglDestroyContext(★첫 후속 커밋 권장, 한 줄 삭제★). 미푸시 — 사용자 결정 대기.
- Task 8: complete (commits fc02de94d 런처마커 + bf70293c4 배압 재제시 + af522092d map실패 폴백, review clean 2차 — Task8 전체 Approved). ★기능 실기 검증 완료: 2×2 색/방향/모션 PASS(스크린샷 판독), 45타일 FAIL→근본수정→PASS(배압 None→appsink 치명에러→45파이프라인 전멸이 원인; 재제시 폴백 후 lockstep/메모리 플랫/루프 무결), WebGPU 무회귀(shared-handle 경로 보존), PROF copy 0.22-0.42ms. 증거 PNG 6장 .superpowers/sdd/evidence/. ★스펙 이탈(문서화 필요): 배압·map실패 시 드롭 대신 링 재제시(스펙 §4.1은 단순 드롭); ring_epoch vestigial. 잔여(공개된 수용 리스크): 시작 창구간 미발행/map실패 None(teardown 계열, 시작 한정), 피크 시 lockstep ≤3프레임 트레일(우아한 드롭). Minor(이월): Ctrl+F12 WR 수치 미채집(구조적 보장으로 갈음), drop_count 주석 드리프트, 만성 기아 비PROF 가시성.
- Task 7: complete (commit 191742723, review clean 1차 승인·픽스 0). 게이트: 안전 크레이트 체크 + mach EXIT 0. 리뷰어 검증: 수명주기 대칭(I420→NV12 id 축소 포함 누수 0), plane dims 프로듀서와 byte-일치, update_plane→update_images 순서 전 경로, 구 타입 grep 0 생존, purge 공통 프롤로그 이동 확인, media_frame_yuv_image 리팩터 raw 경로 무변화 증명. TextureHandle이라 descriptor stride 미설정(정확한 판단). Minor(이월): dims max(0) 클램프 비대칭, 타입 전환 시 스테일 바인딩(raw 경로와 동일 관례), 부분 키 할당 누수(기존 raw와 동일), current_frame_holder 유지(무해).
- Task 6: complete (commit 445e544c1, review clean 1차 승인·픽스 0). 게이트: cargo check -p servo-media-thread + mach EXIT 0(2:18). always-commit 리뷰어 브랜치별 재검증 통과, lock/unlock 균형 airtight(insert-before-increment, ring_id 단조증가), 캐시 스테일 레이스 차단, 플립은 binding_for 단일 술어(paint lib.rs:722-724)로 구경로보다 개선. ★핸드오프: Task 7용 API = allocate_plane_ids/update_plane/remove_plane/binding_for + D3d11PlaneBinding{ring_id,ring_epoch,plane_index,width,height}. Minor(이월/Task7 처리): purge를 공통 lock 프롤로그로 이동(마지막 D3D11 비디오 정지 후 텍스처 잔류 방지), rc 메서드 패닉 시 커밋 스킵은 Task2 계약 책임(정보성). ★ops 함정 추가: Start-Job은 부모 셸과 함께 죽음 — 분리 빌드는 Start-Process+내부 env 소싱.
- Task 5: complete (commit f0ccd4ff3, +564/−1763, review clean 1차 승인·픽스 0). 게이트: 크레이트 테스트 3/3 + mach EXIT 0(4:10). 삭제 완전성 grep 0히트(ffi.rs/interop.rs/예제 통삭제, libloading 워크스페이스까지 제거). ★핸드오프: 프로듀서는 claim 후 publish 무조건 실행(copy_planes 무오류 설계 — Writing 누수 구조적 불가); ring_epoch은 항상 1(vestigial) — ★렌더러 무효화는 ring_id 기준★; consumer_device 미publish 동안 전 프레임 드롭(Task 6이 열쇠). Minor(이월): 전 plane map 실패 시 저품질 프레임 publish(abandon 아님), epoch 주석, E2E 색검증은 Task 8로, div_ceil 스타일.
- Task 4: complete (commits 4c703baf8 + fab7c9fca 가드/계약rustdoc/오용테스트 픽스, review clean 2차). 테스트 10/10, mach build EXIT 0(4:07). ★핸드오프: d3d11_ring pub API는 task-4-report.md §2가 정본(ConsumeCommit=enum, RemappedPlane 신설); 소비자 계약=플랜 Some이면 실패해도 정확히 1회 commit_consume(빈 Vec도 유효 커밋), lock/unlock 짝 맞춤; RemovedRing.mapped는 Presenting 제외(이미 unmap됨). 게이트 대체: cargo check --workspace 불가(mozjs_sys) → mach build. Minor(이월): InitialMapAll 방어경로의 ring-wide apply_mapped(도달불가), .expect 스타일, set_consumer_device 커버리지.
- Task 3: complete (commits 23fcc50b1 + 26bcd823a GL에러 게이트화 픽스, review clean). ★PoC 게이트 4/4 PASS — RTX A5000 실기(GetDesc 검증, WARP 아님), 강화 판정 포함 3회 재현. DYNAMIC R8/RG8 EGLImage 래핑+샘플 정상, DISCARD rename 투명. HALT 불필요.★ 실행법: cargo run --release -p servo-paint-api --example d3d11_dyn_poc --features no-wgl. Minor(이월): UnregisterClassW 미호출, 창 생성 assert 패닉, WARP 프로그램적 거부 없음. ★별도 이월: vendored surfman context.rs:176-177 기존 버그 — eglDestroyContext 이중 호출(WindowRenderingContext Drop 시 AV; PoC는 process::exit 우회, 리뷰어 독립 검증).

## Plan: 2026-07-14-dcomp-mixed-content (DComp 복합 콘텐츠 지원)
스펙: docs/superpowers/specs/2026-07-14-dcomp-mixed-content-design.md
계획: docs/superpowers/plans/2026-07-14-dcomp-mixed-content.md (8태스크, HALT 2곳: T1 G2 실패 시 부분 Present 폐기, T6 WR급 판정 시 보고)
베이스: b31327a45 (스펙+계획 커밋 완료 시점)

## Tasks (mixed-content)
- Task 1: complete (commits d8b1c57cb..d5967b82e, review clean 2차 Approved). ★PoC 게이트 4/4 PASS — FLIP_SEQUENTIAL 컴포지션 스왑체인에서 GetBuffer(1)이 CopySubresourceRegion 소스로 동작(G2 HALT 해제 = 부분 Present GO), Present1 더티렉트 화면 정확, G4 로테이션 = 2버퍼 핑퐁(Present 후 GetBuffer(0)=2 Present 전 내용) — 계획 stale 부기 방향 그대로.★ Minor(최종 리뷰 이월): 프레임A 준비 실패가 exit 2로 광역화(문서화됨), G4 read_pixel Map 실패 시 exit 0, G3 인라인 Present1 분기 페이로드 프리픽스 누락.
- ★ops(이 계획 전체 적용): 계획의 `cargo test -p servo-paint --lib dcomp`는 이 머신에서 불가 — 정확 명령 = `cargo test -p servo-paint --lib --features paint_api/no-wgl dcomp` (surfman ANGLE feature가 servoshell unification에만 있던 것) + zeroize derive 자체선언 매니페스트 픽스 f0267e34e (mach 빌드 무영향). 이후 태스크 디스패치에 이 명령을 명시할 것.
- ★계획 텍스트 결함 교정: Task 2 브리프의 MAX_STALE_RECTS 붕괴 테스트(40회 순차 present)는 붕괴 분기를 못 탐(교대 clear로 stale 누적 불가; 실제 트리거 = 단일 프레임 더티 >32렉트). 계획 의도(붕괴 검증)에 맞춰 단일 on_present(40렉트) 테스트로 교정 — 의미론 자체는 정상(리뷰어 시뮬레이션 검증). 부수 확인: 매 Present 교대라 stale은 항상 '직전 프레임 더티' 1세트 — 누적은 미Present 프레임(on_present 미호출)에서만 유지.
- Task 2: complete (commits 49360fda8 + f0267e34e(컨트롤러 매니페스트) + cbcc1818b(리뷰픽스), review clean 2차 Approved). region_subtract/subtract_rect(4밴드 정확 분해, euclid 터치=비겹침 확인)/StaleTracker(on_present/catchup_rects/reset) + 테스트 6/6. 리뷰어 시뮬레이션 독립검증. 핸드오프: Task 4는 MAX_PRESENT_DIRTY_RECTS/MAX_STALE_RECTS/StaleTracker 소비(allow(dead_code) 제거).
- Task 3: complete (commit 7a11c0265, review clean 1차 Approved, 픽스 0). 승격 위생: MIN_AGE 30(strict >) + 쿨다운 게이트(frame_counter >= promote_blocked_until) + frame_drawn_partial(사용-후-리셋 순서 검증됨) + 신규 필드 4종 전 생성지점 초기화(생성지점 각 1곳 grep 확증). =surface 무변경 확인. Minor(이월): MIN_AGE 주석 한 줄 영어(기존 영어 블록에 덧붙임 — 브리프 지시), frame_counter가 root_visual 가드 뒤라 '합성 활동 프레임' 카운터임(쿨다운 산술에 무해, 주석 후보).
- ★ops 추가: mach build는 servo_env.ps1 소싱 후 `. .\.venv\Scripts\Activate.ps1`도 필요(시스템 python에 toml 없음 — Task 4에서 발견). 이후 빌드 디스패치에 포함할 것.
- Task 4: complete (commits 0a90c9112 + 756ee21ea 리뷰픽스, review Approved 2차). ★부분 Present 실증: 45타일 월 present-partial 478회/withhold 0/실패 0/2.5분 재생. 3x3 재확인(promote+3s, content-swap partial_present=true).★ 리뷰픽스 3건: content-swap 전면 stale 시딩(!content_attached 조건 — SetContent 실패 창구간도 커버, 리뷰어가 제안보다 나은 스코프로 검증), 전면 Present 실패 시 frame_dirty 복원(부분 분기 오염 불가 확인), present1_partial 클램프-후-검사. ★발견: mixed demo =1 비디오 검정은 베이스라인(7a11c0265)에도 동일 — stash A/B 검증, Task 6 진단 범위.★ Minor(Task 5로 흡수 권장): 지속 Present 실패 시 frame_dirty 무한 성장(복원 의미론상 — MAX_STALE_RECTS 붕괴 적용 후보) + Present 실패 warn!이 per-frame(기존, 디바이스로스트 폭주 클래스). Minor(이월): regen 영구 차단 시 frame_dirty 누적(동결 상태라 외견상 무해).
- Task 5: complete (commits 4e8a67c42 + 8c49983f9 리뷰픽스, review Approved 2차). ★강등 실기 검증: 타입2(full=120)+타입1(full=33) 모두 확인, 3사이클 지수 쿨다운 300→600→1200 벽시계 일치(&cycle=1 페이지 모드 추가), no-env 대조 0 demote, 시딩 후 픽셀 정확+갱신 지속.★ 구현자가 전면 Present 실패 warn 무한루프+미강등 갭을 자발 발견·수정(warn-once+즉시 강등). 리뷰픽스: buffer0 완전성 주석 경로별 정정(catch-up 실패 = 1프레임 구본, 스펙 §6.2 수용 인용), 쿨다운 중 streak 누적 중단(스펙 §6.3 정합 — 만료 후 신규 3프레임 streak 필요, 첫 승격 blocked_until=0 무영향 검증), 타입1 전체-타일 복사 불변량 문서화. 유닛 8/8(신규 2: 쿨다운/붕괴). Minor(이월): 불변량 주석 case-b 문구 미세 부정확(coverage.is_full⇒covered_tiles 전 타일 — 결론은 정당), 디바이스로스트 실주입 미실시(코드 리뷰+순수 로직 테스트로 갈음).
- Task 6: complete (commits 22f6ca594 + 1f6874bf4, review Approved 2차 — 판정·증거 적대적 검증 통과). ★결함② 판정 = 우리층 2건 세트: (A) cull_covered 불건전 — WR is_opaque=backdrop 힌트일 뿐 클립 피복 보장 아님; 하트비트 점 2x2만 그리는 above 슬라이스(probe surf4, readback 77/77 dirty=2x2)·상단바 실콘텐츠 슬라이스(demo surf6, 전면 클립+상단 스트립만 painted)가 하위 비디오/티커를 컬링(surf0 1012x). (B) ★AddVisual(TRUE, NULL) = MS 문서상 형제들 아래 렌더 → :2055가 z 전체 역전★(월/quad는 유효 visual 1개라 무증상; 티커 drawn 2816회+화면 0, NO_CULL 상단바 소실 전부 설명). 수정 = (B) insertAbove FALSE 1플래그 + (A) A-1 제거 vs A-2 dense-tile 술어(측정 판별: probe surf4 1타일 탈락/demo surf6 상단행 탈락/월 통과) — 월 cull 실측으로 결정. NO_CULL=probe·demo 완전 복원 인과 A/B(양팔 readback off). readback 계측(SERVO_DCOMP_READBACK, unbind, frame<=120)+dcomp_alpha_probe.html(?case=&hb=) 자산화, mixed_media_demo.html 최초 커밋. 증거표는 리뷰어가 전수 독립 검증(8서피스 테이블 로그 대조, MS 문서 원문 fetch). Minor(이월): hb=0 문구 "미생성"→"생성만, add/bind 0" 정정 필요, 티커 스캔 좌표 미기록, surf2 정체 기하 추론 각주.
- Task 6b: complete (commits 2d886c83d z-역전 픽스 + b4ea92c18 A-2(폐기) + c09817cc8 A-1 컬링 제거, review Approved 2차). ★(B) AddVisual insertAbove TRUE→FALSE — MS 문서 NULL 레퍼런스 의미론 주석 보존. (A) 1차 A-2(dense-tile 술어)는 리뷰어가 Critical 적발: 가상공간 extent(~16384) vs 디바이스공간 clip 비교로 영원히 미발동(테스트는 단일 좌표계라 맹점) → 컨트롤러 결정으로 A-1 전환(근거: is_opaque 비보장 + 월 정상상태 cull 0/시작 커버 42건뿐 + 좌표계 이중성 제거). cull_covered/cull_disabled/SERVO_DCOMP_NO_CULL 완전 제거(+23/-126), frame_surfaces=Vec<NativeSurfaceId>, 재도입 방지 한국어 주석. 검증: probe 3케이스+mixed demo 양모드(그리드/시계/티커/자막 전부)+3x3 월 클린, 유닛 7/7.★ Minor(이월): 주석 괄호가 v1(42건, 유효)과 A-2기(구조적 0) cull=0을 미세 혼동, 6b 리포트 헤더 상단에 최종상태 포인터 부재, 구 계획 문서에 NO_CULL 잔존(역사 문서라 허용). 이월 관찰: =surface 전용 backdrop 아티팩트(반투명 자막 뒤 비디오 대신 검정 — 기존 결함, =1 무영향, 프로덕션 무영향).
- Task 7: complete (코드 무변경 검증, review Approved — 컨트롤러 문서 동기화 후). ★수용 기준 전 항목 PASS: mixed demo =1/=surface 전 요소(영상 6·시계·티커·자막) 45초 동결 0, quad 양팔(기본=무강등/NO_PARTIAL_PRESENT=3사이클 강등·쿨다운 300→600→1200), 45타일 5분 소크(메모리 플랫 4.7GB, lockstep 45/45, PresentMon Composed: Flip 2575/2575=100%, catch-up: 전면 더티 프레임 복사 0 실측·부분 프레임 슬리버 1~16 정상), off/=surface 무회귀, 유닛 7/7.★ 리뷰어가 원시 로그 5종 대조 — 전 수치 byte-exact 확인. ★함정 기록: 45타일 fps "0.33" 소동 = 측정 앨리어싱(샘플 간격 30.37s ≈ 소스 루프 주기 900프레임 — 카운터 1랩; 랩 보정 29.96/29.9fps, 컨트롤러 신규 20s 측정 on=30.0/off=30.05fps 확정). ★버닝된 카운터로 fps 잴 때 샘플 간격이 루프 주기 배수면 안 됨.★ 이월: =surface 전용 캡션/티커 backdrop 검정(티커바까지 확장 관찰, hybrid 무영향).
- Task 8: complete (commit 5c346205e — 스펙 §11 구현결과(PoC 4/4·부분Present 실측·강등 실측·결함② 실제원인/선행스펙 컬링 기능반전 명시·수용기준 6항 결과·이월)+ 선행 스펙 §5.3 위 상호참조 1줄). 패키징: D:\ServoWallPackage 갱신(servoshell.exe HEAD c09817cc8 빌드 02:42:34 교체, mixed_media_demo.html+dcomp_alpha_probe.html 추가, run_wall.ps1 AMD 가이드에 "3중 A/B는 순수 비디오 월 페이지로/mixed_media_demo는 정확성 전용" 2줄 추가) → zip 재압축 1,201,766,516B, 4개 핵심 엔트리 타임스탬프 확인. ★브리프 "12커밋"이 stale로 확인(Task 6b 3커밋 이후 미갱신) — 정확한 수치 15커밋(d8b1c57cb..c09817cc8 inclusive)으로 정정해 스펙에 기록.★ 남은 것: 최종 whole-branch 리뷰(컨트롤러) → 사용자 푸시 게이트.
- Task 8: complete (commit 5c346205e). 패키지 재생성: D:\ServoWallPackage.zip 1,201MB (2026-07-15 03:51, exe=HEAD 빌드 150MB + mixed_media_demo.html + dcomp_alpha_probe.html + run_wall.ps1 AMD 가이드 2줄 추가 — 성능 A/B는 순수 월로, mixed는 정확성용). 스펙 §11 구현 결과 + 선행 스펙(swapchain-content §5.3)에 컬링 제거 상호참조. 커밋 범위 정정: d8b1c57cb..c09817cc8 = 15커밋(+docs 5c346205e).
- 최종 whole-branch 리뷰(fable): Ready to merge **YES** (b31327a45..2c3732dde, 18커밋). Important 2건(리젠 후 강등 제3상태 데드엔드 / geometry_changed frame_dirty 무한성장) 픽스 웨이브 2be65dd02로 해결, 확인 재리뷰 통과(healthy 서피스 wedge 불가 검증). Minor 트리아지 전건 처리(이월 승인 or 픽스). 문서 정합 2c3732dde. 사이클 종료 2026-07-15 — 푸시는 AMD 실측 후 사용자 결정(총 28커밋 로컬: 선행 10 + 이번 18).
- Task 9 (애드온, 사용자 티커 잔상 리포트): complete (commits 908b99a94 본픽스 + 49bd70b37 강건화, review Approved — WR 0.68 소스 대조 검증). ★근본원인 = 가설(H1 원점보정) 아님 — 알파 슬라이스의 '한 번도 안 그려진 밴드' 노출: WR 네이티브 경로는 서피스 클립을 타일 valid 합집합 사각형으로 줌(composite.rs:1033/1043)인데 Draw 경로는 타일별 클립(renderer:3737-39) → 미기록 가상 서피스 메모리(검정/이전 텍스트/파랑)가 합성됨. 수정 = !is_opaque && valid==dirty 타일에서 update rect를 tile∩clip로 확장+투명 클리어(Draw 경로 등가; valid⊋dirty 지속 콘텐츠는 불변). 강건화: 클리어를 소유 영역 시저 제한(아틀라스 이웃 커밋 파괴 방지)+FBO 0 명시 바인딩(잔류 텍스처캐시 FBO 함정). 검증: 티커 단일 사본 스크롤/배경 균일/잔상·복제·파랑 소멸(60s+), 월·프로브 무회귀, 유닛 8/8.★ ★잔여(사용자 결정 대기): 우측 하단 ~17px 검정 스트립 = 별개 원인 — 불투명 바-배경 슬라이스의 타일 (1,2)를 WR이 bind 자체를 안 함(bind 0회) → 불투명 서피스 구멍. 알파 픽스가 노출(원인 아님). 우리 층 깨끗한 수정 불가 1차 판정 — 조사 vs 수용 대기.★ Minor(이월): 클립이 직전 프레임 것(정적 바는 무해), transform_offset round ≤1px 투명 과충전.
- Task 10/10b/10c (애드온, 티커 결함 완결): ★티커 밴드 이상 = 서로 다른 근본원인 3건으로 최종 분해·전부 해소★ ① 알파 미기록 밴드(Task 9, 기록됨) ② 검정 = 유령 불투명 슬라이스: 텍스트 겹침으로 타일들이 알파 동반 슬라이스로 분류돼 불투명 서피스는 타일 1개만 bind되는데 visual 클립은 WR 공유클립(슬라이스 전체 valid 유니온)이라 미도색 영역이 DComp 풀 메모리를 불투명 노출(재활용 텍스트/제로) → 수정 = add_surface에서 불투명 서피스 클립을 실제 bind 타일 valid 유니온으로 정제(Draw per-tile 클립 등가, 커밋 0de120e0b, 유닛 9/9 신규 refine_opaque_clip 테스트, 검정 61→0 양모드) ③ 프로즌 잔상 = 시작 창 리사이즈 스테일: ★원인은 런처 자체 — 실행 후 SetWindowPos가 $WindowSize를 외곽 rect로 재적용해 클라이언트 1920x1080→1904x1041 중도 축소★ → WR-valid 스테일 형성, 새 레이아웃에 프리미티브 없는 구역은 영원히 재bind 안 됨(Task 10b가 FORCE_PICTURE_INVALIDATION 레버 무효를 WR 소스 인용으로 실증 — picture.rs:2375-81, 플래그는 더티 재계산만 강제, 커밋 없이 원복) → 수정 = 런처가 AdjustWindowRectEx로 장식 델타 계산해 클라이언트 최종 크기로 생성·이동만 수행(커밋 d9bce0072 + D:\ServoWallPackage\run_wall.ps1 미러) → 잔상 9404→0(84s/107s 안정), =surface 클린, 월 무회귀. ★함정: DPI 미인식(AdjustWindowRectExForDpi 필요할 수 있음 — 단일 스케일 rig는 무관); 런타임 리사이즈 시 가상 서피스 스테일은 여전히 이론상 가능(월 시나리오 무관, 알려진 한계로 기록)★.
- Task 11 (애드온, 3.5h 크래시): complete (commits 598ed9cdd 재현페이지 + de1b323b9 stylo 벤더링/[patch] 11크레이트 + 6a1846fb7 경계수정, review Approved 1차·픽스 0). ★근본원인: stylo animation.rs get_property_declaration_at_time — 종단 가드는 f64 `total_progress == 1.0` 정확비교인데 키프레임 탐색은 `as f32` 비교 → f64 (1-2^-25, 1.0) 값이 1.0f32로 반올림되면 가드 통과+position()=None → debug_unreachable!(release=unreachable_unchecked UB) → poison 인덱스로 computed_steps[len] 패닉. 수정 A(근본)=가드를 탐색과 동일한 (as f32)>=1.0로 — 리뷰어가 '탐색 None ⟺ 가드 참' 완전 동치 증명, 정상경로 무회귀. 수정 B(심층방어)=None-arm을 종단 키프레임 안전 폴백으로(unsafe 완전 제거; 100% 스톱 없는 키프레임셋에서 실제 도달 가능 — 가치 있음). Reverse 방향은 구조적 면역(첫 키프레임 sp=0 → 임계 1.0 항상 충족).★ RED 129초(300 div 위상 스캔, 프로덕션 시그니처 동일) → GREEN 소크 30분41초 패닉 0(14.3×), 데모/스트레스 회귀+자막 페이드 휘도 사이클 검증. 벤더링 무결성: diff -rq로 checkout 2453c41과 animation.rs 단 1파일 차이 확증, Cargo.lock에 stylo git 소스 잔존 0. ★함정: stylo 업스트림 rev 갱신 시 third_party/stylo 재복사+수정 재적용 필요; cargo git checkout 직접 수정은 rebuild 안 됨([patch] 필수)★. Minor(이월): 리포트 'Fix B 도달불가' 문구 부정확(100% 스톱 부재 시 도달), 주석 내 라인 참조 구버전, debug_unreachable dep 선언 잔존.
- Task 12/12b (애드온, 런타임 리사이즈): ★리사이즈 잔상+드래그 중 블랙 해소.★ Task 12(5abf14c6f, review Approved): 리사이즈 감지+10프레임 디바운스 → SetPictureTileSize로 픽처 캐시 전량 재구축(WR 소스 확증: render_backend.rs:1139-41 → picture.rs:2297-2332 destroy_compositor_surface+tiles.clear — 10b에서 무효였던 FORCE_PICTURE_INVALIDATION과 달리 물리적 재구축). 시계 잔상 1012px→0, 16연속 리사이즈→발사 1회, 무리사이즈 실행 발사 0. kill switch SERVO_DCOMP_DISABLE_RESIZE_REBUILD. Task 12b(51db7c2fe): 드래그 중 = resize_active 모드 — 전 스왑체인 즉시 강등(쿨다운 미적용·(no cooldown) 로그)+승격/리젠 억제(가상 서피스가 드래그 담당) + 드래그 시작 시 재구축 1회 → 드래그 중 regen 51→2/withhold 44→0/영상 재생 유지. 드리프트 픽스(cfa25648c, Task 12 리뷰 Important): 시작 발사=대체값, 안정화 발사=기동 정상상태(env Some 또는 None — 스크롤바 타일 특수크기 복원) → 수렴 실증(기본→1024x512 복귀, env 1920x1080 보존). 12b+픽스 리뷰 진행 중. ★함정: 리사이즈 없는 경로 비용 0 필수(발사 0 검증); -TileSize A/B와의 상호작용은 settle=env 수렴으로 보존★.
- Task 12b 최종: complete (51db7c2fe + cfa25648c 드리프트픽스 + fb2b15ecb RED결합해제, review Approved 2차). 최종 구조: 시작-재구축(B)=마스터 게이트만(SERVO_DCOMP_DISABLE_RESIZE_REBUILD), 가상모드(A: 전 스왑체인 강등+승격/리젠 억제)=추가로 SERVO_DCOMP_DISABLE_RESIZE_VIRTUAL 게이트 — RED 모드에서도 Task 12 재구축 보존(20x create_surface 웨이브 실증). 타일 크기 결정론 수렴: 시작=대체값(512x512), 안정화=기동 정상상태(env Some 또는 None — 스크롤바 특수 크기 복원). 리뷰어 검증: 3중 스왑체인 상태 전부 안전(제3상태 포함), 쿨다운 의미론(리사이즈 강등 무페널티·병리 쿨다운 생존), Cell 배선 동일 스레드 1프레임 자가치유, resize_active RED 도달불가 증명. Minor(이월·수용): RED 라벨 뉘앙스(재구축 2웨이브/드래그), 리사이즈 시딩 실패 프레임당 재시도, 디바이스로스트 중 latch 엣지, 사전 demote_blocked 서피스는 settle까지 동결.

## 2026-07-17 비디오 WR 탈출 사이클 (plan 2026-07-17-video-wr-escape.md)
- Escape Task 1: complete (commit b8515b129, review Approved 1차, Critical/Important 0). 게이트 video_escape_mode(OnceLock)+파서 테스트, 레이아웃 yuv 플래그(off시 바이트동일), 런처 -VideoEscape. Minor(이월): 소비 경로는 paint_api::rendering_context:: 정규화 경로 사용(크레이트 루트 re-export 없음 — Task 5 주의), 레이아웃 주석 한국어 1줄(허용). 함정 재확인: 서브에이전트 백그라운드 빌드 고아 종료 → 컨트롤러 Start-Process detached 필수 + 잔존 servoshell.exe가 링크 잠금(os error 5) → kill 후 재링크.
- Escape Task 2: DONE_WITH_CONCERNS(코드 변경 없음, 검증 전용, RTX A5000 실기). 상세 escape-task-2-report.md. ★Step1(월45타일 native 승격): PASS — create_surface off=4→native=49(**+45**, 정확히 그리드 1칸 크기 213x216/214x216·전부 opaque=true), promote 46(비디오45+콘텐츠1). Step2(콘텐츠 타일 무효화 정지): PASS(강한 정량) — 하트비트 없는 콘텐츠 서피스 bind 총량 off 23,519회→native 2회(★약 11,760배 감소★), 하트비트 보유 서피스도 off 6,555→native 1,756(약 3.7배, 하트비트 잔존분만 남음 = 브리프 예측과 정확히 일치). Step4(off 무회귀+=surface 호환): 둘 다 PASS — off는 45타일 카운터 전원 일치(lockstep 픽셀 확인), =surface(-DComp -DCompSurface, 브리프 원문에 -DComp 누락되어 있어 보정 실행) create_surface=40(비디오36+콘텐츠4), **promote=0**(설계대로 스왑체인 승격 없음), WARN 0 — 프로모션이 스토리지 모드 무관 공통 경로임을 실측 재확인.★ ★Step3(mixed_media_demo/complex_media_stress): 시계·티커·로고·자막 등 비디오 외 전 요소 정상(2초 간격 스크린샷으로 진행 확인) — 단 **CONCERN 발견**: complex_media_stress.html의 PiP(알파 패널 위 오버레이 비디오, `#pipv`)가 native 모드에서 시작 직후(프레임 약 2)에 내장 카운터가 멈춤(60s/62s 캡처 동일값), 반면 동일 페이지를 -VideoEscape 없이(off) 재실행하면 PiP 포함 13영상 전부 완벽 lockstep 진행(269→332/2s) — **원인이 -VideoEscape native로 명확히 격리됨**. 로그 추적: PiP 전용 서피스(tile_size=358x201로 그리드 비디오들과 구분됨)는 정상 생성·스왑체인 승격·끝까지 bind(swapchain) 5,721회 반복(정상 그리드 비디오 5,722회와 동일 빈도, withhold 0, WARN 0) — 즉 프레젠트 파이프라인은 계속 정상 작동하는데 그 안의 픽셀(디코드 프레임)만 갱신이 멎음. 페이지 자체 주석이 이 경로를 "구 결함 경로"로 표시해둔 자리(§3-w/x/y 비불투명 슬라이스 비디오 계열)와 같은 취약점 재발로 보임 — 프로모션 실패는 아니므로 브리프의 BLOCKED 기준 미해당, 그러나 Task 3(interop) 착수 전 별도 트리아지 권고.★ 부수발견(결함 아님, 진단 주의사항): 프로젝트 표준 스크린샷 기법(.NET CopyFromScreen)이 **native로 승격된 비디오 화면을 항상 검정으로 캡처**함(swapchain/virtual surface 모두 동일 — off 대조군·비디오 아닌 오버레이는 정상 캡처되므로 결함이 아니라 DirectComposition 네이티브 서피스가 BitBlt 합성 표면과 별도 경로라는 방증) — Task 6(A단계 통합 검증)도 동일 기법 사용 시 똑같이 검정만 보게 되므로 로그/내장 카운터 기반 검증 또는 대체 캡처 API(PrintWindow PW_RENDERFULLCONTENT 등) 전환 필요. 함정 재확인: 런처가 -Page 무관하게 항상 ?cols=&rows=를 URL에 붙이므로 mixed_media_demo/complex_media_stress처럼 그 값을 읽는 커스텀 페이지는 -Cols/-Rows 명시 필수(안 하면 런처 기본 8x6=48이 페이지 그리드 수를 바꿔버림 — 최초 시도에서 실제로 발생, 폐기 후 재실행).
- Escape Task 2: complete-with-findings (commit 4e2784432, 검증 전용). PASS: 45타일 +45 create_surface(213x216 opaque)/콘텐츠 타일 bind 23519→2/mixed demo 전요소/off·=surface 무회귀. ★결함 발견: stress 페이지 PiP(알파패널 위 비디오) native 모드 프레임 동결(서피스 present 지속·콘텐츠 스테일, off 정상) — 트리아지 진행. ★방법론: BitBlt/CopyFromScreen은 승격 비디오 픽셀 못 봄(검정) — Task 6 검증은 대체 캡처(PrintWindow PW_RENDERFULLCONTENT 후보) 필수.
- PiP 트리아지 확정: (b) WR native-경로(PREFER만, WR이 그리는 모드)의 갱신 층 결함 — off 정상/hybrid 동결/=surface 동결 3중 대조, 우리 컴포지터는 전면더티 4372회 무결 제공. READBACK 진단은 스왑체인 승격 후 관측 불가(설계 한계) 기록. ★함의: external 모드는 WR draw 우회라 이 층 안 탐 — Task 6에서 stress PiP 라이브니스 명시 검증 필수, Task 7 AMD 가이드에 native=진단용+PiP류 캐비앳 문서화★. 이월: WR report_promotion_failure는 DebugFlags 미설정으로 dead code(경고 0이 무의미).
- Escape Task 3: complete (commit 91813794c, review Approved 1차, Critical/Important 0). 리뷰어가 mach build --release 완주(성공 2:28)로 script 크레이트 검증 해소. Minor(이월): d3d11_ring 테스트 take_removed_rings 전역 드레인 병렬 플레이크(선재, --test-threads=1 100%) — 후속 티켓 후보. 게이트 off 스모크는 Task 6 회귀 항목으로 이월(등록 코드 inert).
- Escape Task 4: complete (commit 9784eaf37, review Approved 1차, Critical/Important 0). VideoConvertPass: HLSL 1-draw+상수표 수치검증, SwapDeviceContextState 격리 전경로 확인, WARP 5/5+dcomp 14/14. Minor(이월): 테스트 코드 COM 미해제(프로세스 수명 무해), D3DCompile blob 이론적 누수, painter.rs 선재 clippy 에러(:806 unwrap — 이 사이클 무관).
- Escape Task 5: complete (commit 206067629, review Approved 1차(opus), Critical/Important 0, mach build 3m43s 성공). External storage+stub 3종+add_surface 분기+세대 dedup — 리스 짝맞춤/COM 수명/Commit 원자성/하이브리드 격리 전 항목 소스 검증 통과. Minor(이월): warned_fail 공유 플래그로 실패 유형별 진단 입도 저하, 브링업 로그는 리스 성공 후에만 발화(Task 6 판독 시 유의). 이월 확인 항목(Task 6): ANGLE 상태 격리 실기(SwapDeviceContextState가 composite 중간 실행), 프로모션 실발화.
- ★Escape Task 6: BLOCKED(FAIL) — Gate 1(월 45타일 external)에서 재현 가능한 크래시 발견,
  브리프 STOP 규칙에 따라 중단(코드 변경 0, 상세 escape-task-6-report.md). 머신: RTX A5000,
  빌드 servoshell.exe 2026-07-18 01:40(HEAD 206067629). ★핵심 발견: `-VideoEscape external`이
  규모에 비례해 servoshell.exe를 크래시시킴 — Windows Application 이벤트 로그에 4회 독립
  기록, 전부 `nvwgf2umx.dll`(NVIDIA D3D11 드라이버) 안의 `0xc0000005`(access violation),
  두 fault offset(0x2c778b/0x583728)만 반복 재현(랜덤 손상이 아니라 결정적 경로 시사).
  사다리 재현: 1x1/2x2/3x3 생존(2x2는 SERVO_DCOMP_DEBUG=1 병행 5분+ 무크래시 확인),
  4x4는 지연 크래시(~32초), 5x5/6x6은 즉각 크래시(~2초), 9x5(45타일, Gate 1 본 시나리오)는
  ~7초. **격리 대조군**: 동일 25타일을 `-VideoEscape native`로 실행 시 50초 관측 무크래시,
  게이트 off 45타일도 60초+ 무크래시(WS~5.0GB, 기존 기준치 부합) — 크래시가 서피스/비디오
  개수 자체가 아니라 external 경로(raw D3D11 변환+SwapDeviceContextState+비디오별 flip
  스왑체인) 고유임을 확정. Step 2(브링업 계약 로그)는 안정 규모(2x2)에서 별도 PASS —
  `[dcomp-dbg] external add` 라인의 scale=(1,1)/clip=타일 rect 정확/src=1920x1080 전부
  기대와 일치. 게이트 off 무회귀도 별도 PASS. **Gate 1 FAIL로 Step 3(mixed/stress+PiP
  라이브니스 결정 게이트)/Step 4(10bit/리사이즈/WebGPU/=surface)/Step 5(PresentMon/TileSize)는
  미실시(BLOCKED) — PiP가 external에서 되살아나는지는 이번 세션에서 검증되지 않음.**
  원인 가설(코드 리뷰 기반, 수정은 범위 밖): 락 규율·디바이스 리매핑은 native와 공유돼
  건전(배제됨) — 차이는 external이 비디오마다 프레임당 raw D3D11
  Draw+Present+SetContent 시퀀스를 반복한다는 점, 비디오 수 증가에 따라 크래시가 빨라지는
  패턴은 경합(race) 가설과 정합. **Task 7(패키징) 진행 불가 — A 단계 게이트 전제 미충족.
  근본 수정 후 재검증 필요(최소 재현 커맨드: `-Cols 5 -Rows 5 -Sync -1 -DComp -VideoEscape
  external`, ~2초 내 크래시).**
- external 크래시 디버깅: RESOLVED (commit 061c7f5d0). 근본원인 = VideoConvertPass SRV 캐시가 DYNAMIC plane의 WRITE_DISCARD rename 후 댕글링(드라이버가 해제된 rename 버퍼 참조 → nvwgf2umx AV, 비디오 수에 비례해 가속). 수정 = SRV 캐시 제거, 매 변환 신선 생성+draw 직후 unbind+Release(D3D11 지연 파괴 안전). 검증: 5x5/45타일 각 5.5분 소크 0크래시+lockstep, 15/15, off/native 스모크. ★재발방지: DYNAMIC(WRITE_DISCARD) 텍스처에 뷰 캐싱 절대 금지 — rename마다 뷰가 낡음(ANGLE은 draw마다 뷰 재파생이라 무사고였음). 실험 사다리 11모드 기록 escape-crash-debug-report.md★
- 크래시 픽스 리뷰: Approved(무수정). 부분실패 경로 SRV 정리/unbind-before-release/진단 게이트 완전 원복/실험 매트릭스 판별력(freshsrv vs nosrv 결정쌍) 전부 확인. 관찰: cbuffer 캐시는 뷰 간접 없음이라 동버그 클래스 비해당.
- ★Escape Task 6 재개(2026-07-18): DONE — 근본수정(061c7f5d0) 후 Gate 1~5 + PiP 결정 게이트
  전부 PASS(코드 변경 0, 상세 escape-task-6-report.md "재개(RESUMED)" 섹션). Gate 1(45타일
  external 30분 소크): 인라인 폴링(하네스가 백그라운드 Monitor/Start-Sleep을 차단·고아화할
  가능성 지적받아 `ping -n 300`으로 5분 단위 인라인 대기 반복 — 백그라운드 Monitor도 병행
  가동했는데 이번엔 끝까지 살아남아 교차검증됨)으로 32.3분 관찰: WS 4651~4720MB **플랫**,
  크래시 0, 신규 WARN/ERROR 0. Gate 3(mixed_media_demo 6/6 + complex_media_stress 13/13
  external 승격, PiP 포함 opaque=true): ★PiP 라이브니스 결정 게이트 PASS★ — PiP 영역 정밀
  크롭 비교(t=0/+62초)로 장면 전환+카운터 증가(27st→28st) 직접 확인, native 동결 결함
  재현 안 됨(사이클 요구 충족). 드래그 테스트 predrag 샷에서 그리드 12+PiP **전부**
  동일 카운터로 완전 lockstep도 별도 확인. 4K 회전 페이지는 create_external_surface
  0건으로 의도대로 레거시 폴백. ★부수 발견(방법론 갱신): external 스왑체인은 BitBlt/
  CopyFromScreen으로 실제 캡처됨(native/hybrid와 달리 검정 아님) — 선행 세션의 "BitBlt
  무효" 결론은 native 한정으로 정정, Task 7 이후 스크린샷 1차 수단 재채택 가능.★ Gate 4:
  10bit 색 정상(경로 오타 자가정정: `-Src tests/...`→`-Src ../jellyfish...`), 리사이즈/
  드래그(§3-y, 신규 스크립트 drag_capture_external.ps1) 40스텝 연속 리사이즈 전 구간
  크래시·잔상 0(mid-drag 흰색 과도상태는 정상 catch-up, 정착 후 lockstep 완전 회복) +
  13개 external 스왑체인은 리사이즈 구간 재생성 0건(고정 픽셀 레이아웃이라 창 크기 무관 —
  최상의 억제 결과), WebGPU 월 무회귀(게이트 off/on 둘 다 backend=WebGPU 정상), =surface+
  external 호환(4/4 승격 + 일반 promote=0 설계대로), 게이트 off 45타일 무회귀 전부 PASS.
  Gate 5: PresentMon(관리자, 전면, `--no_track_gpu --no_track_input`로 ETW 유실 회피 —
  최초 시도 421,933건 유실) 46개 스왑체인(비디오45+콘텐츠1) 100% Composed:Flip 확인,
  TileSize 유무 A/B 29.09fps↔30.39fps로 **사실상 무차이**(비디오가 이미 WR 콘텐츠 패스를
  완전히 벗어나 있어 픽처캐시 타일 크기가 관여하지 않음 — TileSize 레시피는 native/hybrid
  전용, external 채택 시 불필요라는 운영 결론). 이월: PiP `border-radius`가 external 사각
  클립으로 대체됨(`rounded clip radii unsupported` warn, 시각적 사소 저하·기능 무관).
  **Task 7(패키징/최종 리뷰) 착수 가능 — A 단계 게이트 전제 충족.**
- Escape Task 6: complete (commit ab6a784d7, 전 게이트 PASS). 30분 소크 WS 4651-4720MB 플랫/크래시 0, mixed 6/6+stress 13/13 external 승격, ★PiP PASS(62s 카운터 진행+lockstep — native 동결은 external에서 비재현 = 복합 무결 충족)★, 4K 회전 폴백 정상, 10bit/리사이즈40스텝(잔상0, 드래그 중 일시 흰 catch-up만)/WebGPU/=surface/off 전부 PASS, PresentMon 46 스왑체인 100% Composed:Flip. ★운영: external이면 TileSize A/B 무차이(29.09/30.39fps) — 티어링 튜닝 불요★. 방법론: external 스왑체인은 BitBlt 캡처 가능. Minor(이월): PiP 둥근모서리 사각클립(rounded clip 미지원, 외관만).
- Escape Task 7: complete (commit cd39397bc — 스펙 12장+패키지 재생성 zip 1201MB+스모크 PASS 4/4 external).
- 최종 whole-branch 리뷰(fable): **Ready to merge YES** (869560a2a..cd39397bc 13커밋, Critical/Important 0). 신규 Minor 3건 후속 티켓 후보: ①attach 미지 서피스 warn 플러드(퇴화 경로, warn-once화) ②SetContent 실패+일시정지 소스 재시도 갭 ③테스트 take_removed_rings ring-id 필터화. 이월 Minor 9건 전부 '이월 수용' 판정. 스펙 12.2 이탈 2건 추가 기록 커밋으로 반영. 사이클 종료 — 푸시/AMD 실측은 사용자 결정.
- 변형 혼재 실증(2026-07-18, complex_media_transforms.html 4376ddb60): external=11/13 정확(회전 2개는 최초 컴포짓부터 미승격, destroy 0 — 혼재 즉시 안정). ★신규 발견: 스케일 애니메이션 비디오(bob 타일 id=14)는 클립 크기가 매 프레임 변해 v1 계약상 스왑체인 재생성 처닝(content-attach 1603회/45s, ~35/s) — 동작 정상·표시 무결, 비용만. translate 전용 애니메이션은 무처닝. 후속 후보: 애니메이션 중 스왑체인 크기 고정+비주얼 스케일.★
- 한글 폰트 근본대응(2026-07-18): ★Servo 폰트 실측 3함정 — ①@font-face 웹폰트 전면 미동작(로컬 OTF 전 형태 실패) ②같은 패밀리 내 font-weight 선택 미동작 ③설치된 Malgun Gothic 이름 매칭 실패(Noto Sans KR/CJK KR은 매칭됨)★. 대응 = 폰트 설치 방식: Noto Regular+CJK Bold 2종 번들, install_fonts.ps1(HKCU+AddFontResourceW, 오프라인·무관리자·무재부팅 — 즉시 인식 실증), 페이지는 본문 Noto Sans KR/굵은요소 Noto Sans CJK KR(Bold 전용 패밀리 분리). 6페이지 적용, 육안검증(제목·티커 Bold 고딕, 13타일 lockstep 000109). 패키지 1163MB 재생성(fonts+installer+페이지). 도구: scratchpad\winshot.ps1(PrintWindow PW_RENDERFULLCONTENT — external 비디오 픽셀 캡처됨 실증).
- AMD external 절벽 대응 1차(2026-07-18, commit 41d462b83, review Approved): 증상=36타일 fps 하락+GPU% 동반 하락(GPU 기아=렌더러 스레드 예산 절벽). 조치 ①SwapDeviceContextState 배치화(프레임당 72→2, 종료점은 start_compositing — WR 소스 검증: passes 루프 타일 GL이 add_surface 루프와 end_frame 사이 실행되므로 end_frame 종료는 ANGLE 상태 오염) ②스왑체인 백버퍼 RTV 캐시(rename 없어 안전 — DYNAMIC SRV 금지 원칙과 구분 주석) ③[vesc-prof] 1Hz 계측(SERVO_VIDEO_ESCAPE_PROF=1). ★핵심 판독(A5000 45타일): present_ms=229/s가 지배(convert 18.6/s) — Present×N이 잔여 구조 비용, AMD 절벽 유력 원인. probe와의 토폴로지 차이(probe=Present 1회/DWM 1서피스 vs 우리=Present N회/DWM N+1비주얼) 재확인 — 공유 비디오 캔버스(underlay 전체를 단일 스왑체인+부분 Present)가 구조 해법 후보★. 패키지 재생성.
- 패키지 전체 재구성(2026-07-18 15:33): 원본 D:\ServoWallPackage 소실(사용자 이관 추정) → 리포 산출물로 재조립. exe(41d462b83 최적화판)+DLL96+resources+페이지7종+미디어3종+fonts+install_fonts.ps1+run_wall.ps1(리포 런처에서 재작성 — vcruntime 체크, 폰트/AMD 3중 A/B/PROF 판독 가이드 헤더, 페이지별 -Cols/-Rows/-Sync 표 포함, logs\ 패키지 내부로). 단독 스모크 PASS(2x2 external 4/4·engaged·패닉0). zip 1160MB. ★run_wall.ps1은 이제 리포에 정본 없음 주의 — 소실 대비로 etc\multigpu에 사본 백업 권장(다음 정리 항목)★. 업데이트 번들 D:\ServoWallUpdate_20260718.zip(68MB)도 별도 유지.
- ★AMD PROF 판독(2026-07-18, vesc_result.txt): Present×N 직렬화 확정★. 30타일: presents ~900/s, present_ms ~600/s(스레드의 60%), 컴포짓 45-52fps, Present당 ~0.67ms. 36타일: 컴포짓 18-26fps로 붕괴, present_ms ~740/s(75%), Present당 ~1.0ms(★초선형 — 드라이버 경합★), converts ~750/s(=비디오 실효 ~21fps로 강하, 링 latest-wins 드롭). convert/acquire는 부차(30-45/25-35ms). GPU% 하락 = 스레드가 Present 드라이버 코드에 갇혀 GPU 기아. A5000 대비 Present당 4-6배(0.17 vs 0.67-1.0ms). ★결론: 공유 비디오 캔버스(underlay 전체 단일 스왑체인+부분 Present 1회/프레임) 사이클 착수 근거 확정 — 30타일도 48fps라 전 구간 이득 예상★

## 공유 비디오 캔버스 사이클 (2026-07-18, plan: docs/superpowers/plans/2026-07-18-shared-video-canvas.md)
- BASE: 542db02b0 (+계획 정리 1커밋). 푸시 보류(사용자 지시). 6태스크.
- Task 1: complete (commits 0cd50e631..a87059126, review clean/Approved). Minor 이월: rendering_context.rs:1972 기존 테스트명 "_only" stale(canvas도 파싱됨) — 최종 리뷰 트리아지.
- Task 2: complete (commits a87059126..82ed73cb4, review clean/Approved). convert()→convert_to_rect 위임 리팩토링 포함. Minor 이월: 테스트 경계 인접 샘플(x=7/8) 미포함 — 최종 리뷰 트리아지. (§5.4 참조는 실존 확인됨 — 컨트롤러)
- Task 3: complete (commits 82ed73cb4..9e0588b97, review clean/Approved). Minor: 리포트 산술 불일치(코드 무관), present1_with_dirty 불필요 allow+원 주석 소실(→Task 4에서 흡수), 소멸 스캔 HashMap 순서 비결정(테스트 함정 주의) — 최종 리뷰 트리아지.
- Task 4: complete (commits 9e0588b97..a38b6a44a, review clean/Approved — opus 리뷰: lease 단일출구/COM수지/배치순서/게이팅 전부 검증). Minor: canvas AddVisual warn 비게이트(관례일치), prof 카운터 과계상(plan-mandated), ★페이지 이탈→복귀 시 더티힌트 엣지(빈자리 rule⑤ 미발동 — 비주얼 트리 재추가 Commit이 커버, Task 5 인지 항목)★ — 최종 리뷰 트리아지.
- Task 5: complete (전 단계 PASS — 상세 .superpowers/sdd/canvas-task-5-report.md[비추적 스크래치, 리포 관례상 미커밋 — 계획 문면의 "리포트 커밋"은 이 원장 기록 커밋으로 갈음]). 수치: 45타일 converts=presents×45, presents/frames 0.62~0.87(더티스킵), ~60fps, PresentMon 스왑체인 46→2·Composed:Flip 100%(1951/1951), 복합3종 PASS(PiP 동결無, 캔버스 재생성 1회 고정=처닝 소멸), 드래그 중 재생성 0회+정착 후 1회, 무회귀 4종+WebGPU PASS, 소크 30분 신규 경고 0(t=25min WS 5158→3264MB 하향은 OS 트림 판정 — 종료 t≈33min 실화면 45타일 30fps lockstep±0 확인). 브리프 결함 2건(→Task 6 문서화): 3x3 축소시 -Sync 10 정확, =surface 조합은 -DComp -DCompSurface 필요. 검증 에이전트 1회 사망→컨트롤러가 소크 승계(증거 디스크 보존).
- Task 6: complete (commits d95015729+c394ba2f2, 재리뷰 Approved). AMD 4자 가이드(canvas 권장)+스펙 §11 결과 기록+패키지 재생성(1,216,854,445B, 07-18 20:04, 스모크 PASS). 픽스 1건: 가이드의 Present 직렬화 수치 A5000 오귀속→AMD(§3-ac) 정정+문장 분리. Minor 이월: run_video_wall 주석 footprint 초과(무해) — 최종 리뷰 트리아지.
- 최종 whole-branch 리뷰(fable): **Ready to merge YES** (Critical/Important 0). 핵심 축(lease 짝맞춤/COM 수지/ANGLE 배치 규율/DXGI 더티 계약/모드 격리) 코드 정독 전부 건전, 페이지 복귀 엣지도 코드로 커버 검증. FIX-BEFORE-MERGE 1건(T1 테스트명) 픽스 완료(7668a041b, 스펙 11.2 문면 이탈 3건 기록 포함). 신규 Minor 이월 4건=후속 티켓 후보: ①draw 실패 프레임 더티 누락(1프레임 자기복구) ②canvas 모드 provider 미등록 무음(warn-once 2줄) ③canvas↔per-video 전환 last_presented 스테일(3중 작위 조건) ④런처 -VideoEscape ValidateSet/확정 모드 로그. CARRY-OVER 유지: T2 경계샘플, T3 소멸 순서 비결정, T4 3건(warn 비게이트/prof 과계상/복귀 엣지=검증됨), T6 주석 footprint. ★사이클 전체 완료 — 푸시는 사용자 보류 지시 유지.★

## 캔버스 opaque 전환 애드온 (2026-07-19, plan: docs/superpowers/plans/2026-07-19-canvas-opaque-alpha.md, spec: 캔버스 스펙 §12)
- BASE: e42c63410. 푸시 보류 유지. 2태스크.
- [opaque] Task 1: complete (commits e42c63410..53dd20d8b, review clean/Approved). Minor 이월: 근접 토큰("1 "/"01") 엣지 테스트 부재(구현은 정확) — 최종 리뷰 트리아지.
- [opaque] Task 2: complete (commits 53dd20d8b..9731c9b42, 픽스 1회 후 재리뷰 Approved). 검증: 45타일 alpha=opaque 1회/lockstep, 불투명 UI 프로브 6/6 diff=0, transforms 재생성 1회, 패키지 재생성+스모크. ★반투명 영역 픽셀 실증은 구조적 불가(두 데모 페이지 모두 반투명 오버레이가 풀블리드 비디오 위) — §12.6.1에 정직 공개, §12.1 구조 논증 의존 명기. 후속 경로: 정적 단색 테스트 클립 1칸 교체 방식(리뷰어 제안) — 이월★. -Page는 리포 루트 상대경로 필수(tests\html\...) 문서 결함 추가 발견. zip 증가분 귀속 정정.
- [opaque] 최종 리뷰(opus): **Ready to merge YES** (Critical/Important 0). 권고 2건(스테일 투명 주석 4곳/12.6.1 포워드 포인터) 즉시 반영(7d181fda2, cargo check 클린). Minor 이월: 근접 토큰 엣지 테스트(구현은 리터럴 매치라 정확). ★opaque 애드온 사이클 완료 — 푸시 보류 유지.★
- [canvas-only] 최종 리뷰(sonnet): With fixes → 픽스 완료(a5df1347b). Important 2건 처리: 가이드 스왑체인 기대치 상충 해소(순수 그리드 페이지=1개 명기), escape 페이싱 갭 백로그 명기(스펙 13.4 말미 — 엔진 self-paced tick/훅 확장이 정본, 페이지 rAF는 임시 규약). FIX-BEFORE-MERGE: 클린 페이지 제목 구분. CARRY-OVER: 가이드 any-mode 일반화 문구(보수 방향·코드 근거 있음). 재패키징(1,216,860,655B급, 13:45). ★canvas-only 애드온 사이클 완료 — 푸시 보류 유지.★

## 자체 페이싱 애드온 (2026-07-19, plan: docs/superpowers/plans/2026-07-19-video-self-pacing.md, spec: 캔버스 스펙 §14)
- BASE: 2f9c8712b. 푸시 보류 유지. 2태스크.
- [self-pacing] Task 1: ★스모크 게이트 FAIL — 계획 전제 오류 확정, 커밋 보류★. 검증된 사실: ①render_d3d11_yuv_frame는 escape 모드에서도 매 프레임 UpdateImage(epoch=None) push(:984, 호환 프레임 경로) — 3-m 훅은 이미 발동 중(신호 추가=중복, ON≈OFF 실증) ②진짜 웨지 = display_composite_in_flight가 임베더 페인트(렌더 패스 :873 / frame_ready need_repaint=false :1512)에서만 해제되는데, rAF 없으면 임베더가 페인트를 안 함 → 첫 컴포짓 후 renderer_behind() 영구 true → 3-m 가드가 전부 차단(frames=1/55s 계측) ③유력 원인층 = servoshell 임베더 wake/스케줄링(3-2 WakeCoalescer·is_animating 계열 — 비디오 전용 재생을 animating으로 안 침). 다음: 스펙 14 메커니즘 v2(임베더 frame-ready wake) 재설계 — 사용자 에스컬레이션.
